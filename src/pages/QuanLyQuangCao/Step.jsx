import React from 'react';
import PropTypes from 'prop-types';

Step.propTypes = {
    step: PropTypes.number.isRequired,
};

function Step(props) {
    const { step } = props
    return (
        <div>
            
        </div>
    );
}

export default Step;